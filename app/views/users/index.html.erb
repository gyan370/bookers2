<div class="container">  
 <div class="row">   

 <div class="col-md-3">
  <%= render 'books/book', user: current_user %>
 </div> 

 <div class="col-md-8 offset-md-1">
  <h1>Users</h1>
 　<% @user.each do |user| %>
  <div>
   <p>image: <%= link_to image_tag(user.get_profile_image(100,100)), user_path(user) %></p>
  　<p>name：<%= user.name %></p>
  
    <%= link_to "Show", user_path(user.id) %>
    
 <% end %>
  </div>
 </div>  
</div>    
